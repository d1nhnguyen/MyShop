<ContentDialog
    x:Class="MyShop.App.Views.Dialogs.EditProductDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Edit Product"
    CornerRadius="8"
    PrimaryButtonText="UPDATE"
    CloseButtonText="CANCEL"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"
    CloseButtonClick="OnCloseButtonClick">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1300</x:Double>
    </ContentDialog.Resources>

    <ContentDialog.PrimaryButtonStyle>
        <Style TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
    </ContentDialog.PrimaryButtonStyle>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="16">
                <TextBox Header="Product Name" x:Name="NameBox" PlaceholderText="e.g. Iphone 14"/>

                <TextBox Header="Description" x:Name="DescBox" 
                         PlaceholderText="Description text..." 
                         Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

                <ComboBox Header="Category" x:Name="CategoryCombo" HorizontalAlignment="Stretch">
                    <x:String>Iphone</x:String>
                    <x:String>Ipad</x:String>
                    <x:String>Laptop</x:String>
                    <x:String>Tablet</x:String>
                    <x:String>PC</x:String>
                    <x:String>TV</x:String>
                </ComboBox>

                <TextBox Header="Brand Name" PlaceholderText="Apple"/>

                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Header="SKU" x:Name="SkuBox" PlaceholderText="#32A53"/>
                    <NumberBox Grid.Column="1" Header="Stock" x:Name="StockBox" Minimum="0" SpinButtonPlacementMode="Inline"/>
                </Grid>

                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <NumberBox Grid.Column="0" Header="Regular Price" x:Name="PriceBox" Minimum="0" SpinButtonPlacementMode="Inline"/>
                    <NumberBox Grid.Column="1" Header="Sale Price" PlaceholderText="Optional" Minimum="0" SpinButtonPlacementMode="Inline"/>
                </Grid>

                <TextBox Header="Tag" PlaceholderText="Best Seller, Trending"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Spacing="20">
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}" 
                        CornerRadius="8" Height="250" 
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" 
                        BorderThickness="1">
                    <Grid>
                        <FontIcon x:Name="DefaultIcon" Glyph="&#xE739;" FontSize="48" 
                                  Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Image x:Name="PreviewImage" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <Grid Height="100">
                    <Rectangle Stroke="{ThemeResource SystemControlHighlightAccentBrush}" 
                               StrokeThickness="1" StrokeDashArray="4,2" RadiusX="8" RadiusY="8" 
                               Fill="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"/>

                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="24" Foreground="{ThemeResource SystemControlHighlightAccentBrush}"/>
                        <TextBlock Text="Change image" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" FontWeight="SemiBold"/>
                    </StackPanel>

                    <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                            Background="Transparent" BorderThickness="0" 
                            Click="OnUploadClick"/>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</ContentDialog>